/* Создайте хранимую процедуру, которая принимает
в качестве входного параметра IN employee_id
и возвращает в качестве выходного параметра 1 или 0:
  - eсли з/п сотрудника выше средней з/п по всем  департаментам – 1,
  - в противном случае – 0. */
