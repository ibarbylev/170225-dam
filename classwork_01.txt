SELECT * FROM northwind.order_details;

-- 01 Найдите общее количество товаров quantity в таблице order_details.



-- 02 Посчитайте количество уникальных order_id в таблице order_details.



-- 03 Перечислите через запятую имена всех сотрудников из таблицы employees.



-- 04 Выведите среднее, минимум и макисиму столбца unit_price таблицы order_details.


-- ===================================== GROUP BY =============================================
SELECT * FROM northwind.employees;

-- 05  Из таблицы employees посчитать количество сотрудников в каждом городе city.




-- 06 Отсортировать результаты по убыванию.



-- 07 Подсчитать общее количество продуктов из таблицы order_details для каждого заказа.
SELECT * FROM northwind.order_details;




/* 08 Отсортировать по убыванию общего количества продуктов. Для краткости записи в GROUP BY
можно не указывать конкретное имя колонки, а указать ее порядковый номер в SELECT. */


-- Сокращённая запись по номеру колонки в SELECT:



 -- 09 Сделать то же самое  в ORDER BY.

/* 10 Посчитать сколько сотрудников работает в каждой компании из таблицы customers.
Учитывать только тех сотрудников, у которых job_title равен 'Purchasing Manager'. */
SELECT * FROM northwind.customers;




/* 11 Если столбец, который вы хотите использовать для группировки содержит только уникальные неповторяющиеся значения,
то в группировке нет смысла - любая агрегатная функция даст один и тот  же результат.
Попробуйте сгруппировать любую таблицу по первичному ключу и применить агрегатные функции к столбцам. */



-- 12 Посчитать количество сотрудников в разрезе компании и занимаемой должности из таблицы employees.



