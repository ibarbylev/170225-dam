
/* Задача 1: Базовая таблица пользователей
Создайте таблицу users с полями:
    id — целочисленный первичный ключ с автоинкрементом.
    username — строка до 50 символов, уникальное значение, не может быть пустым.
    email — строка до 100 символов, уникальное значение.
    age — целое число, должно быть больше 18.
    created_at — дата и время, автоматически устанавливаемые при добавлении записи.
Придумайте примеры для проверки ограничений.
*/



/* Задача 2: Таблица с фиксированными значениями в поле
Создайте таблицу employees, где:
    id — первичный ключ.
    name — строка до 100 символов, обязательное поле.
    position — строка, может быть только одним из значений: 'manager', 'developer', 'designer'
            ('manager' по умолчанию).
    salary — число с двумя знаками после запятой, должно быть больше 0.
Придумайте примеры для проверки ограничений.
 */



/* Задача 3: Проверка диапазона значений
Создайте таблицу products:
    id — первичный ключ.
    name — строка до 100 символов, не может быть пустой.
    price — число с двумя знаками после запятой, должно быть больше 0 и меньше 10000.
    stock — целое число, не меньше 0 и не больше 1000.
Придумайте примеры для проверки ограничений.
*/



/*Задача 4: Дата в определённом диапазоне
Создайте таблицу future_events:
    id — первичный ключ.
    title — строка до 100 символов, обязательное поле.
    event_date — дата, должна быть не раньше 10 января 2025 года.
Придумайте примеры для проверки ограничений.
*/

